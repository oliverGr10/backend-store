# Framework principal
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Base de datos y ORM
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
greenlet==3.1.1

# Supabase (nuevo formato de keys sb_publishable_ / sb_secret_)
supabase>=2.15.0
httpx>=0.27.0,<0.29

# Validaciones y configuración
pydantic[email]==2.10.3
pydantic-settings==2.6.1

# Variables de entorno
python-dotenv==1.0.1

# IA (proveedores intercambiables — configura AI_PROVIDER en .env)
google-genai>=1.0.0      # para AI_PROVIDER=gemini
anthropic>=0.40.0        # para AI_PROVIDER=anthropic

# Autenticación
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilidades
python-multipart==0.0.20

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
